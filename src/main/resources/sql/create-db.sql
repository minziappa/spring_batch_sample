CREATE TABLE one.users (
  id INTEGER PRIMARY KEY,
  name VARCHAR(30),
  email  VARCHAR(50)
);

CREATE TABLE one.home (
  id INTEGER PRIMARY KEY,
  home_name VARCHAR(30),
  home_address VARCHAR(50)
);
